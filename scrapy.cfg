# Scrapy's project marker. The settings module lives inside the installed
# package rather than at the repo root, so the spider is runnable from anywhere
# the package is importable -- including from the Dagster subprocess in Phase 5,
# which does not necessarily share this working directory.
[settings]
default = kedra_scraper.scraper.settings
